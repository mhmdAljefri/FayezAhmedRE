# Migration `20201208232645`

This migration has been generated by Mohammed at 12/9/2020, 2:26:45 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Offer" ADD COLUMN     "countryId" INTEGER NOT NULL

ALTER TABLE "Offer" ADD FOREIGN KEY("countryId")REFERENCES "Country"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201208224820..20201208232645
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -43,11 +43,11 @@
   updatedAt                 DateTime        @updatedAt
   paymentType               PAYMENT_TYPES   @default(cash)
   status                    STATUS          @default(inprogress)
-  country                   Country          @relation(fields: [countryId], references: [id])
+  country                   Country         @relation(fields: [countryId], references: [id])
   countryId                 Int
-  propertyType              PropertyType?    @relation(fields: [propertyTypeId], references: [id])
+  propertyType              PropertyType?   @relation(fields: [propertyTypeId], references: [id])
   propertyTypeId            Int?
   name                      String
   subTitle                  String
@@ -77,8 +77,10 @@
   subTitle                  String
   project                   Project          @relation(fields: [projectId], references: [id])
   projectId                 Int
+  country                   Country          @relation(fields: [countryId], references: [id])
+  countryId                 Int
 }
 model PropertyType {
     id             Int      @default(autoincrement()) @id
```


