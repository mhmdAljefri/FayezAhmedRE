# Migration `20201210212335`

This migration has been generated by Mohammed at 12/11/2020, 12:23:35 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "OprationCompanyPageData" DROP CONSTRAINT "OprationCompanyPageData_countryId_fkey"

CREATE TABLE "OprationCompanyPage" (
"id" SERIAL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "countryId" INTEGER NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "image" TEXT,
    "video" TEXT,
    "constructingUpdateVideo" TEXT,
    "constructingUpdatePrview" TEXT,
    "oprationCompanies" JSONB,

    PRIMARY KEY ("id")
)

DROP TABLE "OprationCompanyPageData"

ALTER TABLE "OprationCompanyPage" ADD FOREIGN KEY("countryId")REFERENCES "Country"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201210211615..20201210212335
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -65,9 +65,9 @@
   locationText              String?
   oprationCompanies         Json?
 }
-model OprationCompanyPageData {
+model OprationCompanyPage {
   id                        Int             @default(autoincrement()) @id
   createdAt                 DateTime        @default(now())
   updatedAt                 DateTime        @updatedAt
   country                   Country         @relation(fields: [countryId], references: [id])
```


